const executor = () => {
    console.log('Description: Array.of(): you give it things & it makes an array out of it');
    console.log('An example array: is', Array.of(1));
    console.log('An example non array: is', Array.of(4,7,3));
};

executor();
